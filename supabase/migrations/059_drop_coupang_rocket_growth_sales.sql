-- ============================================
-- 059_drop_coupang_rocket_growth_sales.sql
--
-- ⚠ 되돌릴 수 없습니다. 대시보드 숫자를 확인한 뒤에 실행하세요.
--
-- 로켓그로스 매출 소스를 coupang_rg_order_lines 하나로 일원화하면서(058 이후)
-- 옛 집계 테이블을 걷어낸다. 소스가 둘이면 결국 어긋나고, 실제로 화면에도
-- 목록이 둘로 보였다.
--
-- 실행 전 확인 (하나라도 어긋나면 실행하지 마세요):
--   · 대시보드 로켓그로스 매출이 2026-05 부터 현재까지 이어서 나오는가
--   · 옛 집계와 새 라인 합계가 기간별로 맞는가
--   · 상품별 판매통계에 로켓그로스 상품이 그대로 나오는가
--
-- 코드는 이미 새 테이블만 읽는다 (rfm-store.listSales). 이 테이블을 지워도
-- 조회가 깨지지 않는다 — 남겨두면 쓰지 않는 데이터가 헷갈릴 뿐이다.
-- ============================================

DROP TABLE IF EXISTS coupang_rocket_growth_sales;
